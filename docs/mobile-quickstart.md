# Mobile Quickstart

This project scaffold was generated for app: `frappe_native`.

## 1) Prepare Android tooling

Install Android Studio / Android SDK and ensure `gradle` is available.

## 2) Build debug APK

```bash
cd apps/frappe_native/mobile/android
gradle wrapper
gradle assembleDebug
```
