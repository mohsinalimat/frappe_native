# Android Client (frappe_native)

This folder is generated by:

```bash
bench native init --app frappe_native --platform android
```

If `gradlew` is missing, open this folder in Android Studio once, or run:

```bash
gradle wrapper
```
